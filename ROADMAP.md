# ChatGPT2LocalBridge Roadmap

Objective: make Web ChatGPT feel like it can safely operate an approved local workspace by calling clear MCP tools instead of guessing shell commands.

## Roadmap Rules

- `[x]` means the item has current evidence in code, docs, tests, or a recorded release.
- `[ ]` means planned, partial, unverified, blocked, or still needs user input.
- Low-level tools stay available for advanced debugging, but product docs and tool descriptions should guide ChatGPT toward safer mid-level and high-level tools first.
- Public deployments should default to OAuth or Secure MCP Tunnel. No Authentication is only for loopback, private lab, or short-lived test tunnels.
- Do not mark broad safety goals complete until the tool catalog, runtime policy, app UI, and deployment docs all match.

## Current Baseline

- The TypeScript OAuth/MCP server is the canonical CLI, launchd, and macOS app engine.
- Startup is fail-closed, filesystem checks use canonical paths, and the default public profile is read-only.
- `repo_*` tools provide Git-complete manifests, paged maps, large-file chunks,
  TS/JS/Python symbols, optional CodeGraph context, branch comparison, coverage,
  and resumable acknowledged scans.
- Low-level write, shell, process, and Codex Runner tools remain available only
  through explicitly broader profiles.

## Current Evidence

- [x] Roadmap exists as the source-of-truth execution plan.
  - Evidence: 2026-06-20 `ROADMAP.md` added to the repository.
- [x] README shows the product architecture as a horizontal visual.
  - Evidence: 2026-06-20 `docs/assets/architecture-horizontal.png` is generated by `npm run docs:assets` and referenced from `README.md`.
- [x] README introduces the low/mid/high tool-tier model.
  - Evidence: 2026-06-20 `README.md` documents low-level debug tools, mid-level project tools, and planned high-level Codex Runner tools.

## Key Decisions

- Keep low-level tools, but label them as advanced/debug tools.
- Promote mid-level project tools for reading context, validating policy, inspecting diffs, and running tests.
- Route complex work through structured handoffs before local Codex execution. See [Handoff Spec](./docs/handoff-spec.md).
- Add high-level `codex.*` tools so Web ChatGPT can delegate local software work to Codex CLI through a controlled runner.
- Treat local skills as a safe registry first, then a manifest-driven runtime. See [Skill Runtime Roadmap](./docs/skill-runtime-roadmap.md).
- Default public guidance to OAuth or Secure MCP Tunnel.
- Disable or hide raw `shell.exec` in normal user mode when a safer tool can handle the same workflow.
- Make the native/local app the operator console for policy, traces, running tasks, logs, diffs, and cancellation.

## Tool Tiers

### Low-Level Debug Tools

Purpose: direct primitives for advanced troubleshooting.

- `file.read_path`
- `file.write`
- `shell.exec`

Policy:

- [ ] Mark these tools as advanced/debug in descriptions, docs, and the app catalog.
- [ ] Add warnings when ChatGPT calls `shell.exec` for tasks that should use `codex.task_start`, `test.run`, or `project.bundle`.
- [ ] Support a normal mode where `shell.exec` is disabled or omitted from the public connector tool list.
- [ ] Keep an explicit debug mode for trusted local development.

### Mid-Level Project Tools

Purpose: inspect and operate a project without forcing ChatGPT to compose shell commands.

- `project.bundle`
- `policy.read`
- `git.diff`
- `test.run`

Policy:

- [ ] Make `project.bundle` the recommended first call for multi-file context.
- [ ] Add or harden `git.diff` so ChatGPT can inspect local changes without raw shell.
- [ ] Add or harden `test.run` so common test commands are policy-controlled and traceable.
- [ ] Ensure every mid-level tool writes a structured tool-call record.
- [ ] Add examples that tell ChatGPT to use mid-level tools before low-level tools.

### High-Level Codex Runner Tools

Purpose: give Web ChatGPT one clean entry point for local coding work.

Initial tool set:

- `handoff.create`
- `codex.task_start`
- `codex.status`
- `codex.result`

Target tool set:

- [x] `handoff.create`: validate and persist a structured task handoff without executing code.
  - Evidence: 2026-06-21 `docs/handoff-spec.md`, `src/mcpServer.ts`, and `scripts/test-mcp.sh`.
- [ ] Harden `codex.task_start`: start a bounded local Codex CLI job in an approved root with production-grade runner controls.
- [ ] Harden `codex.status`: return lifecycle state, elapsed time, active step, and last log lines.
- [ ] Harden `codex.result`: return summary, changed files, diff, tests, and artifacts.
- [ ] `codex.cancel`: stop a running job from ChatGPT or the local app.
- [ ] `codex.tail`: stream or page recent logs for long jobs.
- [ ] `codex.review_diff`: ask local Codex CLI to review current changes without applying edits.

Runner constraints:

- [ ] Require `projectRoot` to be inside `allowedProjectRoots`.
- [ ] Enforce per-task timeout and output limits.
- [x] Run Codex CLI with the intended local runner permission mode.
  - Current mode: `--sandbox danger-full-access --dangerously-bypass-approvals-and-sandbox`.
  - Boundary still comes from bridge policy, handoff constraints, timeout, logs, and App trace.
- [ ] Store task logs, stdout/stderr, generated patches, and final summaries in the runtime data directory.
- [ ] Record all runner lifecycle events in `tool-calls.jsonl` and audit logs.
- [ ] Expose changed-file and diff summaries before any public or destructive operation.

## Security Plan

### Public Access

- [ ] Use OAuth for normal public tunnel deployments.
- [ ] Use OpenAI Secure MCP Tunnel when available to avoid exposing a public MCP URL.
- [ ] Keep No Authentication limited to loopback, private lab, or temporary test endpoints.
- [ ] Document that a no-auth public tunnel is a capability URL and should be treated as sensitive.

### Shell Safety

- [ ] Make `shell.exec` opt-in for normal deployments.
- [ ] Add a policy switch for `publicToolProfile`: `normal`, `debug`, or `codex-runner-only`.
- [ ] In normal mode, prefer `codex.*`, `project.bundle`, `git.diff`, and `test.run`.
- [ ] Apply deny patterns before shell execution and record blocked attempts.
- [ ] Add app UI controls for enabling/disabling shell access with visible risk copy.

### Policy Editing

- [ ] Move policy editing into the local app UI.
- [ ] Validate roots, deny globs, skill roots, shell settings, and auth mode before saving.
- [ ] Warn when broad roots such as `/`, `$HOME`, `~/.codex`, or `/Volumes` are approved.
- [ ] Show exactly which roots are visible to ChatGPT.
- [ ] Add one-click export of the active policy without secrets.

## App Upgrade Plan

### Operator Console

- [ ] Show connector health, public URL state, auth mode, and current MCP tool count.
- [ ] Show the recommended tool tier for each tool.
- [ ] Show full tool-call traces with request, response, duration, status, and error.
- [ ] Show file read/write records and cloud download records.
- [ ] Add filters for `file.*`, `project.*`, `git.*`, `test.*`, `codex.*`, and `shell.*`.

### Codex Runner Panel

- [ ] Start a local Codex task from the app.
- [ ] Show running task logs.
- [ ] Stop a running task.
- [ ] Show changed files and diff.
- [ ] Open the project folder.
- [ ] Copy a ChatGPT-ready prompt that targets `codex.task_start`.

### Policy Center

- [ ] Edit approved workspace roots.
- [ ] Edit skill roots, defaulting to `~/.codex/skills` instead of the whole `~/.codex`.
- [ ] Toggle shell access.
- [ ] Validate policy before saving.
- [ ] Restart the bridge from the app after policy changes.

## Documentation Plan

- [ ] Add a "Tool Tiers" page explaining low-level, mid-level, and high-level tools.
- [ ] Add a "Use ChatGPT with Local Codex CLI" tutorial.
- [ ] Add OAuth setup screenshots for Mac mini and Linux.
- [ ] Add Secure MCP Tunnel setup path when available.
- [ ] Add No Authentication warning page for temporary tests only.
- [ ] Add troubleshooting examples for when ChatGPT uses Python instead of the connector.
- [ ] Add prompt templates that force use of `project.bundle` or `codex.task_start`.

## Test Plan

- [ ] Unit test tool registration and schema for all `codex.*` tools.
- [ ] Integration test `codex.task_start -> codex.status -> codex.result`.
- [ ] Verify blocked roots are rejected.
- [ ] Verify shell-disabled profile does not expose or execute `shell.exec`.
- [ ] Verify OAuth connector can list and call `codex.*` tools.
- [ ] Verify no-auth test mode works only in explicitly configured lab mode.
- [ ] Verify app trace view shows every MCP call made by ChatGPT.

## Release Milestones

### Milestone 1: Tool Catalog Cleanup

- [ ] Classify all current tools by tier.
- [ ] Update tool descriptions to nudge ChatGPT toward safer tools.
- [ ] Generate an updated tool catalog.
- [ ] Update README and docs with the tier model.

### Milestone 2: Safe Mid-Level Workflows

- [ ] Harden `project.bundle`.
- [ ] Implement or finalize `git.diff`.
- [ ] Implement or finalize `test.run`.
- [ ] Add trace records for all mid-level tools.
- [ ] Add ChatGPT test prompts for local read, diff, and test workflows.

### Milestone 3: Skill Runtime MVP

- [ ] Stabilize the read-only skill registry.
- [ ] Add `skill.json` manifest parsing.
- [ ] Add manifest-aware `skill.list/search/read/route`.
- [ ] Add safe template-only `skill.invoke`.
- [ ] Add App Skill Catalog and trace grouping.

Source of truth: [Skill Runtime Roadmap](./docs/skill-runtime-roadmap.md).

### Milestone 4: Handoff -> Codex Runner MVP

- [x] Add `docs/handoff-spec.md`.
- [x] Implement `handoff.create`.
- [x] Let `codex.task_start` accept `handoffId`.
- [ ] Harden `codex.task_start` for real Codex CLI jobs.
- [ ] Harden `codex.status` for long-running task polling.
- [ ] Harden `codex.result` for final summaries, changed files, and artifacts.
- [ ] Persist logs and results.
- [ ] Add timeout and root restrictions.
- [ ] Validate with a real local repo task.

### Milestone 5: App Control Plane

- [ ] Add policy editor.
- [ ] Add trace viewer.
- [ ] Add Codex Runner task panel.
- [ ] Add shell access toggle.
- [ ] Add connector setup helper.

### Milestone 6: Public-Safe Deployment

- [ ] Make OAuth the default public guide.
- [ ] Add Secure MCP Tunnel guide.
- [ ] Keep No Authentication documented only as temporary lab mode.
- [ ] Add release audit for secrets, private paths, and unsafe defaults.

## Open Questions

- [ ] Should `shell.exec` be hidden from `tools/list` in normal mode, or listed but blocked by policy?
- [ ] Should `codex.task_start` run synchronously for short tasks or always return a task id?
- [ ] Which Codex CLI profile should be the default for runner jobs?
- [ ] Should the app support multiple machines, such as Mac mini and Linux server, in one dashboard?
- [ ] Should skill discovery be exposed through separate `skill.*` tools or only through `project.bundle` and `codex.*` prompts?
